defaultNamespace: netdata

helm:
  chart: github.com/netdata/helmchart/charts/netdata
  releaseName: netdata
  values:
    service:
      type: NodePort
    ingress:
      enabled: false

targetCustomization:
  - name: dev
    clusterGroup: dev
    helm:
      values:
        service:
          type: ClusterIP